import * as React from "react";
import usePromiseEffect from "../../src"

export default function App() {
    usePromiseEffect(() => {

    }, () => {

    }, [])
    return (
        <div>
        </div>
    )
}